language: go
go:
  - 1.13.x
gobuild_args: -v -race
services: mysql
before_install:
  - mysql -e "CREATE DATABASE IF NOT EXISTS test;"
env:
  - DB_HOST=127.0.0.1
  - DB_PORT=3306
  - DB_NAME=test
  - DB_USER=root
